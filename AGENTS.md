# AGENTS.md

## Cursor Cloud specific instructions

This is a greenfield "stock trading app" repository. As of the initial commit, no application code, frameworks, dependencies, or services exist — only `README.md` and `LICENSE`.

### Current state

- **Language/framework**: Not yet chosen.
- **Package manager**: None configured.
- **Services**: None to start.
- **Tests**: None to run.
- **Lint**: No linter configured.

### Notes for future agents

- When application code is added, update this file with build/run/test instructions.
- Update the VM environment update script (via `SetupVmEnvironment`) once dependency files (e.g. `package.json`, `requirements.txt`) are introduced.
